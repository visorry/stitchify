import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["images.squarespace-cdn.com", "i.etsystatic.com", "plus.unsplash.com", "via.placeholder.com", "images.unsplash.com", "source.unsplash.com"], // Add the domains you need here
  },
};

export default nextConfig;
